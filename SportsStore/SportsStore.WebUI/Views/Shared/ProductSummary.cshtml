@model SportsStore.Domain.Entities.Product

<div class="card bg-light">
    <h3>
        <string>@Model.Name</string>
        <span class="float-right badge badge-primary">@Model.Price.ToString("c")</span>
    </h3>
    <span class="lead"> @Model.Description</span>
</div>